@using IvtLibrary.Helpers
@model IvtLibrary.Research

@{
    ViewBag.Title = "Исследования";
}

<h2>Добавление</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Исследование</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.name, "Название")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.name)
            @Html.ValidationMessageFor(model => model.name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.description, "Подробное описание")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.description)
            @Html.ValidationMessageFor(model => model.description)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.Author, "Авторы исследования")
        </div>
        <div>
            @Html.CheckBoxList("authorIds", (List<SelectListItem>)ViewBag.AuthorsList)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.Theme, "Тематика исследования")
        </div>
        <div>
            @Html.CheckBoxList("themeIds", (List<SelectListItem>)ViewBag.ThemesList)
        </div>
        <p>
            <input type="submit" value="Добавить" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Назад к списку", "Index")
</div>
